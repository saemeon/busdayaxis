[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mplbusdayaxis"
version = "0.1.0"
description = "Matplotlib business day axis scale"
readme = "README.md"
license = {text = "BSD-3-Clause"}
authors = [
    {name = "Simon Niederberger", email = "s.niederberger@hotmail.com"}
]
requires-python = ">=3.9"
dependencies = [
    "matplotlib",
    "numpy",
    "pandas",
]

[project.optional-dependencies]
test = [
    "pytest",
    "pytest-mpl",
    "pytest-cov",
]
doc = [
    "mkdocs",
    "mkdocs-material",
    "mkdocs-gallery",
    "mkdocstrings[python]",
]
dev = [
    "ruff",
    "pre-commit",
    "pytest",
    "pytest-mpl",
    "mkdocs",
    "mkdocs-material",
    "mkdocs-gallery",
    "mkdocstrings[python]",
]

[tool.pytest.ini_options]
testpaths = ["src/mplbusdayaxis/tests"]

[tool.ruff]
line-length = 88
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pre-commit]
default_stages = ["pre-commit"]

[tool.pre-commit.hooks]
ruff = {args = ["check", "--fix"]}
ruff-format = {args = ["format"]}
